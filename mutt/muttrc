set header_cache="$HOME/.cache/mutt"
set tmpdir="$HOME/.cache/mutt/tmp"
set folder="imaps://$OUTLOOK@outlook.office365.com:993"
set attach_save_dir="${tmpdir}"

set spoolfile="+INBOX"
set record = "+Sent"
set postponed = "+Drafts"
set trash = "+Trash"

set imap_user="$OUTLOOK"
set imap_authenticators="oauthbearer:xoauth2"
set imap_oauth_refresh_command="python $HOME/.config/mutt/mutt_oauth2.py ${header_cache}/${imap_user}.token"
set message_cachedir="${tmpdir}"

set smtp_url="smtp://${imap_user}@smtp-mail.outlook.com:587"
set smtp_authenticators=${imap_authenticators}
set smtp_oauth_refresh_command=${imap_oauth_refresh_command}

set ssl_force_tls=yes

set send_charset="utf-8"
set assumed_charset="iso-8859-1"
set editor="vim -c 'set syntax=mail ft=mail enc=utf-8'"

set sort=reverse-date-received
set sort_aux=threads
set strict_threads=yes

set realname="Carlos Vieira"
set from="${imap_user}"

# mailboxes we want to monitor for new mail
mailboxes "="

# folder-hook
# color
# save-hook
# fcc-hook
# send-hook
# reply-hook
# crypt-hook
# cd
# set copy
# set default_hook
# set folder_format
# set header_cache_compress
# set imap_check_subscribed
# set index_format
# set mail_check
# set mailcap_path
# set new_mail_command
# set pager
# set signature
