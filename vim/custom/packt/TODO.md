TODO
* [ ] list all packages currently installed
    * [ ] grouped by 'group'
    * [ ] show the total number of plugins installed, the number of plugins in `start`/`opt` folders
    * [ ] show a visual sign indicating if the plugin is optional or not
    * [ ] show a visual sign indicating if the plugin has its documentation available
* [ ] update all packages (mainly through git)
    * [ ] perhaps add a step of post install similar to how vimplug does
* [ ] move a plugin from the `start` folder to the `opt` folder and vice-versa
* [ ] rename the 'group' folder where many plugins reside (ex:
`/path/to/vim/pack/GROUP_A/plugin` -> `/path/to/vim/pack/GROUP_B/plugin`)
* [ ] create a new plugin in a group
* [ ] delete an entire group
* [ ] delete a plugin from a group
* [ ] start an optional pack (basically calling packadd pkgname)
* [ ] generate the documentation of a plugin
* [ ] generate the documentation of all plugins
